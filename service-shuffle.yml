server:
  port: 8000
  
spring:
  cloud:
    consul:
      discovery:
        instanceId: ShuffleService
        health-check-interval: 5s
      config:
        management: 
          enabled: true
    stream:
      bindings:
        output:
          binder: kafka
          destination: cloudEX
management:
  security: 
    enabled: false
